import { RemoveFirstCharPipe } from './remove-first-char.pipe';

describe('RemoveFirstCharPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveFirstCharPipe();
    expect(pipe).toBeTruthy();
  });
});
